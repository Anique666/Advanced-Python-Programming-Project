# Google Street View API Configuration
# REQUIRED: Get your API key at: https://console.cloud.google.com/
# Steps:
# 1. Create a new project
# 2. Enable "Street View Static API"
# 3. Create an API key in Credentials
# 4. Enable billing on your project (free tier covers ~1,000 images/month)
GOOGLE_STREETVIEW_KEY=your_google_api_key_here

# JWT Token Secret
# REQUIRED: Generate a strong random string for security
# You can generate one with: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET=your_strong_random_secret_here

# Backend URL
# Used by frontend to communicate with backend
# Default is fine for local development
BACKEND_URL=http://localhost:8000
